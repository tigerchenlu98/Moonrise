source "https://rubygems.org"

# Use the GitHub Pages stack (pins Jekyll + supported plugins)
gem "github-pages", group: :jekyll_plugins

# Required when running Jekyll on Ruby 3.x locally
gem "webrick", "~> 1.8"

